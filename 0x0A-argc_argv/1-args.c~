#include <stdio.h>
#include "main.h"
/**
* main - Entry point
* @argc: argument count
* @argv: array vector
* Return: Always (0) Success
*/
int main(int argc, char *argv[])
{
if (*argv != '\0')
{
printf("%d\n", argc);
}
return (0);
}
