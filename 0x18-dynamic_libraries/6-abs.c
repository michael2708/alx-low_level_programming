#include "main.h"

/**
 * _abs - prints the absolute
 * @n: character to check
 * Return: absolute
 */

int _abs(int n)
{
if (n > 0)
{
return (n * 1);
}
else if (n == 0)
{
return (0);
}
else
{
return (n * -1);
}
}
